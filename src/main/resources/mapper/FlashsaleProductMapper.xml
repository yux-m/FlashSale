<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="yuxm.flashsale.mapper.FlashsaleProductMapper">

    <!-- mapping results -->
    <resultMap id="BaseResultMap" type="yuxm.flashsale.pojo.FlashsaleProduct">
        <id column="id" property="id"/>
        <result column="product_id" property="productId"/>
        <result column="flashsale_price" property="flashsalePrice"/>
        <result column="stock_count" property="stockCount"/>
        <result column="start_date" property="startDate"/>
        <result column="end_date" property="endDate"/>
    </resultMap>

    <!-- result list -->
    <sql id="Base_Column_List">
        id
        , product_id, flashsale_price, stock_count, start_date, end_date
    </sql>

</mapper>
